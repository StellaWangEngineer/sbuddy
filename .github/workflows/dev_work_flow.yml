name: CI/CD for dev branch

on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
  deploy: 
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Create private key file
        run: echo ${{secrets.DEV_SSH_PRIVATE_KEY}} > private_key

      - name: Connect to Dev server
        run: ssh -i private_key ${{secrets.DEV_SERVER_USERNAME}}@${{secrets.DEV_SERVER_IP}} "ll"


      
